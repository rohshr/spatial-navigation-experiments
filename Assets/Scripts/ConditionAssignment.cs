using UnityEngine;

public class ConditionAssignment : MonoBehaviour
{
    /*
        3 conditions
        1: Continuous movement (c)
        2: Free teleportation (t)
        3: Node-based teleportation (n)

        Possible permutations of conditions:
        ctn, cnt, tcn, tnc, nct, ntc
    */

    // objects: 1234; n! = 24
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
